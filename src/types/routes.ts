export enum Routes {
  Conference = '/conference',
  ConferenceCreate = '/',
  ConferenceLeft = '/conference-left',
}

export enum CreateStep {
  username = 0,
  meetingName = 1,
  deviceSetup = 2,
}
